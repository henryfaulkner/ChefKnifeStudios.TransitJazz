The project's .sln namespace is ChefKnifeStudios.TransitJazz and the folder to the git directory is C:\Projects\ChefKnifeStudios.TransitJazz. The PokerAttack's directory path is C:\Projects\ChefKnifeStudios.PokerAttack. I am creating a new project, which is separate from the ChefKnifeStudios.PokerAttack codebase. This new project, while entirely separate from the ChefKnifeStudios.PokerAttack project, will have a very similar structure. I would like to use spec-driven development to accelerate the initialization of the project. The solution components should all be instramented with Aspire. All projects will use .NET 10. It will have a Blazor WASM frontend client with a 3-tiered project structure: Core, Shared, and WebApp. The Blazor frontend structure should come with a SignalR client, a EventNotificationService (for client-side eventing), should use the same color set as PokerAttack, and MatBlazor. It will have an ASP.Core app which doubles as an API and a SignalR Hub. The ASP.Core app will use the Clean Architecture .NET methodology. The ASP.Core app should come with some logging pattern, a Minimal API group, a SignalR Hub, and Scalar installed. The ASP.Core should have a Dockerfile to create containerized image. The solution should have a docker-compose file in the root. There should be a cross-domain Shared project for DTOs, Enums, and shared Constants. The project should have a separate deployment pipeline for client and server. Try to match PokerAttack's folder structure one-for-one.